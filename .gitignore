# Internal planning notes — MUST NOT be published (firewall: they reference
# AI(2)M(2)IA / the "Nimbus" bank). Keep them locally, out of the public repo.
docs/pt-book-coverage-*.md

# macOS
.DS_Store

# Python
__pycache__/
