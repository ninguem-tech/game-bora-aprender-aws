# Internal planning notes — MUST NOT be published (firewall: they reference
# nin-guem / AI(2)M(2)IA / the "Nimbus" bank). Keep them locally, out of the
# public repo.
docs/pt-book-coverage-*.md

# Working/source notes used to draft the supplement question files — same
# private-by-convention treatment as the planning notes above.
data/sources-cache/

# Configuração local de ferramentas de IA (permissões da máquina; não publicar)
.qwen/

# macOS
.DS_Store

# Node.js
node_modules/

# Python
__pycache__/
